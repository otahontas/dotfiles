# Config files are symlinked so don't add them
fontconfig/conf.d/*.conf

# Don't add files or folders that mainly contain just auth tokens and temp files
pulse/
hub
spotify-tui/
syncthing/
vdirsyncer/
